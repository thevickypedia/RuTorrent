/// Module for the project's build-time variables.
pub mod constant;
/// Module for all shared settings.
pub mod settings;
/// Module for all the pre-configured default values.
pub mod defaults;
/// Module for all environment variables, and their associated settings.
pub mod env;
